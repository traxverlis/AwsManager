<UserControl x:Class="AwsManager.Views.Route53View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:AwsManager.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:Route53ViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="600" d:DesignWidth="1200">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" IsIndeterminate="True" />
                <TextBlock Text="Loading..." FontSize="16" Margin="10,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Hosted Zones List -->
        <DockPanel Grid.Column="0" IsEnabled="{Binding !IsLoading}">
            <TextBlock DockPanel.Dock="Top" Text="Hosted Zones" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="5"/>
            <Button DockPanel.Dock="Top" Content="Refresh All" Command="{Binding RefreshCommand}" Margin="5,0,5,5"/>
            <ListView ItemsSource="{Binding HostedZones}" SelectedItem="{Binding SelectedHostedZone}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Id}" Foreground="Gray" FontSize="10"/>
                            <TextBlock Text="{Binding Comment}" FontStyle="Italic" TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </DockPanel>

        <!-- Resource Record Sets -->
        <DockPanel Grid.Column="1" IsEnabled="{Binding !IsLoading}">
            <StackPanel DockPanel.Dock="Top" >
                <TextBlock Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="5,5,5,0">
                    <Run Text="Records for:"/>
                    <Run Text="{Binding SelectedHostedZone.Name, FallbackValue='(No Zone Selected)'}" FontWeight="Bold"/>
                </TextBlock>
                <StackPanel Orientation="Horizontal" Margin="5">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="IsEnabled" Value="True"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedHostedZone}" Value="{x:Null}">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <Button Content="Create Record" Command="{Binding CreateRecordCommand}"/>
                    <Button Content="Update Selected" Command="{Binding UpdateRecordCommand}" Margin="5,0"/>
                    <Button Content="Delete Selected" Command="{Binding DeleteRecordCommand}"/>
                </StackPanel>
            </StackPanel>
            <DataGrid ItemsSource="{Binding ResourceRecordSets}" 
                      SelectedItem="{Binding SelectedRecordSet, Mode=TwoWay}"
                      AutoGenerateColumns="False" IsReadOnly="True" Style="{StaticResource MaterialDesignDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
                    <DataGridTextColumn Header="TTL" Binding="{Binding TTL}"/>
                    <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>
