<Window x:Class="AwsManager.Views.Dialogs.EditSGRecordSetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewmodels="clr-namespace:AwsManager.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodels:EditSGRecordSetViewModel, IsDesignTimeCreatable=True}"
        Title="Edit Record Set"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        Width="600">
    <StackPanel Margin="16">
        <TextBox Text="{Binding Name}" materialDesign:HintAssist.Hint="Record Name" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,5"/>
        <ComboBox ItemsSource="{Binding RecordTypes}" SelectedItem="{Binding Type}" materialDesign:HintAssist.Hint="Record Type" Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="0,5"/>
        <TextBox Text="{Binding Ttl}" materialDesign:HintAssist.Hint="TTL (Seconds)" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,5"/>
        <TextBox Text="{Binding Value}" 
                 materialDesign:HintAssist.Hint="Value (one entry per line)" 
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Height="150"
                 Margin="0,5"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Save" IsDefault="True" Click="SaveButton_Click" Margin="0,0,8,0"/>
            <Button Content="Cancel" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
